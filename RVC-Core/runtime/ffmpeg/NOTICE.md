# FFmpeg 运行依赖

本目录包含 RVC Studio 在 Apple Silicon macOS 上使用的静态 FFmpeg 9.0.1 二进制：`ffmpeg` 与 `ffprobe`。

这两个文件是 arm64 Mach-O 可执行文件，不是 RVC 模型权重。FFmpeg 按 LGPL-2.1-or-later 许可发布；完整许可文本与源码获取方式请参阅 <https://ffmpeg.org/legal.html>。

本项目不提交任何 RVC 推理模型、预训练模型、用户模型、索引、训练特征、训练音频或训练 checkpoint。
