# Third-party notices

The bundled `pymss` and `pymss_core` source trees are derived from the
[pymss project](https://github.com/pymss-project/pymss). Their license texts are
preserved at `pymss/LICENSE` and `pymss_core/LICENSE`.

Model checkpoints remain subject to the licenses and terms published by their
respective model authors and distribution source. A release distributor must
review those terms before redistributing the checkpoints. The application can
place legitimately obtained checkpoints in `assets/pymss_models` without
depending on a system PyMSS installation.
